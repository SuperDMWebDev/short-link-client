export const userSelector = state => state.users.user
export const tokenSelector = state => state.users.token

export const urlSelector = state => state.links.url
export const shortUrlSelector = state => state.links.shortUrl
export const listSelector = state => state.links.listUrl